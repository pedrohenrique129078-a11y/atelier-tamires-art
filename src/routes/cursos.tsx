import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight } from "lucide-react";
import { PageShell } from "@/components/page-shell";
import { Button } from "@/components/ui/button";
import { courses } from "@/lib/site-data";

export const Route = createFileRoute("/cursos")({ head: () => ({ meta: [{ title: "Cursos presenciais — Tamires Correia" }, { name: "description", content: "Formação e especialização presencial em manicure, SPA dos pés e cuidados podais." }, { property: "og:title", content: "Formação & Especialização — Tamires Correia" }, { property: "og:description", content: "Conheça os cursos presenciais de Tamires Correia." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: "/cursos" }] }), component: CoursesPage });
function CoursesPage() { return <PageShell eyebrow="Formação presencial" title="Técnica que se aprende de perto." intro="Onze formações presenciais para diferentes momentos profissionais. O conteúdo abaixo preserva as informações fornecidas."><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><div className="divide-y divide-border border-y border-border">{courses.map(([name, description, duration], i) => <article key={name} className="grid gap-5 py-8 md:grid-cols-[64px_minmax(0,1fr)_auto] md:items-start"><span className="font-serif text-2xl text-primary">{String(i+1).padStart(2,"0")}</span><div className="min-w-0"><h2 className="text-2xl md:text-3xl">{name}</h2><p className="mt-2 text-sm font-medium text-primary">{duration}</p><p className="mt-4 max-w-3xl text-sm leading-6 text-muted-foreground">{description}</p></div><Button asChild variant="quiet" size="lg"><Link to="/agendar" search={{ tipo: "curso", curso: i }}>Agendar curso <ArrowUpRight /></Link></Button></article>)}</div></section></PageShell>; }